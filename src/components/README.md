# CV React – Esteban Bedoya

Proyecto desarrollado en React utilizando Vite, como parte del ejercicio práctico solicitado
para reforzar el uso de *renderizado condicional, **renderizado de listas*, y el uso de
*Git/GitHub* mediante commits organizados.

El proyecto incluye:

- Componente *Experiencia.jsx* con 10 cargos y proyectos renderizados desde un arreglo.
- Componente *Educacion.jsx* que muestra formación académica y complementaria de forma dinámica.
- Componente *StackTecnologias.jsx* con etiquetas de tecnologías y colores condicionales para cada tipo.
- Navegación simple y secciones visualmente organizadas.

---

## 🚀 Cómo ejecutar este proyecto

1. Clonar el repositorio:
   ```bash
   git clone https://github.com/esteban01Br/cv-react-esteban-bedoya.git
   ```

2. Instalar las dependencias:
   ```bash
   cd cv-react-esteban-bedoya
   npm install
   ```

3. Ejecutar el proyecto:
   ```bash
   npm run dev
   ```

4. Visitar la aplicación en el navegador: http://localhost:5173/



git add .
git commit -m "feat: componente CabeceraCV con datos personales actualizados"
bash

git add .
git commit -m "feat: componente Perfil con descripción personalizada"
bash

git add .
git commit -m "feat: componente Experiencia dinámico con 10 cargos definidos"
bash

git add .
git commit -m "feat: componente Educación dinámico con formación complementaria"
bash

git add .
git commit -m "feat: componente StackTecnologías dinámico y con renderizado condicional"
